FORMATTABLE_STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx|json)$')

for FILE in $FORMATTABLE_STAGED_FILES; do
    npm run format "$FILE"
    git add "$FILE"
done

npm test
